---
  - name: Configuring zsh...
    command:
      cmd: "chsh -s /bin/zsh {{target_user}}"
    become: true

  - name:
    copy:
      src: "{{target_home_dir}}/arch-build/roles/setup/configurations/zsh/files/.zshrc"
      dest: "{{target_home_dir}}/.zshrc"
      owner: "{{target_user}}"
      group: "{{target_user}}"
      mode: '0644'
    become: true
